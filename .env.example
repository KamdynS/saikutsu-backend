# Database connection
DATABASE_URL=postgres://localhost/saikutsu

# Supabase JWT secret (from Supabase dashboard → Settings → API → JWT Secret)
SUPABASE_JWT_SECRET=

# Allowed origins for CORS (comma-separated, leave empty for allow-all in dev)
ALLOWED_ORIGINS=
